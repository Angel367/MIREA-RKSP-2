CREATE TABLE IF NOT EXISTS mamontsr (
                                     id SERIAL PRIMARY KEY,
                                     name VARCHAR(255),
                                     color VARCHAR(255),
                                     age INTEGER
);